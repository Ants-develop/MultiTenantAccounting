// Messenger Hook
// Convenience hook for accessing messenger context

import { useMessenger as useMessengerContext } from "@/contexts/MessengerContext";

export const useMessenger = useMessengerContext;

